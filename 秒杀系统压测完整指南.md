# 秒杀系统压测完整指南

## 1. 压测前准备

### 1.1 环境检查
```bash
# 检查Java环境
java -version

# 检查MySQL服务状态
# 确保数据库服务正常运行

# 检查Redis服务状态  
# 确保Redis服务正常运行

# 检查JMeter安装
jmeter --version
```

### 1.2 数据库准备
```sql
-- 1. 导入初始数据
source sql/miaosha.sql;

-- 2. 检查用户数据
SELECT COUNT(*) FROM miaosha_user;  -- 应该有5000个用户

-- 3. 检查商品数据
SELECT * FROM goods;
SELECT * FROM miaosha_goods;

-- 4. 重置秒杀商品库存（压测前执行）
UPDATE miaosha_goods SET stock_count = 1000 WHERE id = 1;
UPDATE miaosha_goods SET stock_count = 1000 WHERE id = 2; 
UPDATE miaosha_goods SET stock_count = 1000 WHERE id = 3;
UPDATE miaosha_goods SET stock_count = 1000 WHERE id = 4;

-- 5. 清理历史订单数据
DELETE FROM miaosha_order;
DELETE FROM order_info;
```

### 1.3 应用服务配置
```properties
# application.properties 压测专用配置
# 数据库连接池配置
spring.datasource.hikari.maximum-pool-size=50
spring.datasource.hikari.minimum-idle=20
spring.datasource.hikari.connection-timeout=30000

# Redis连接池配置  
spring.redis.jedis.pool.max-active=50
spring.redis.jedis.pool.max-idle=20

# JVM参数
-Xms4g -Xmx4g -XX:+UseG1GC -XX:MaxGCPauseMillis=200
```

### 1.4 生成用户Token（重要！）
```bash
# 1. 确保应用已启动
# 2. 运行UserUtil生成token
cd miaosha-v1
# 编译项目
mvn clean compile

# 运行UserUtil生成tokens
java -cp target/classes:lib/* com.geekq.miaosha.utils.UserUtil

# 这会生成 tokens.txt 文件，包含1000个用户的token
```

## 2. 压测场景设计

### 2.1 基准性能测试
**目标**: 测试系统正常负载下的性能表现
- **并发用户数**: 100
- **测试时长**: 10分钟
- **测试接口**: 商品列表、商品详情
- **期望指标**:
  - 平均响应时间 < 500ms
  - 95%响应时间 < 1000ms
  - 错误率 < 0.1%
  - TPS > 500

### 2.2 秒杀压力测试  
**目标**: 测试秒杀接口的极限性能
- **并发用户数**: 1000-5000（递增）
- **测试时长**: 2分钟
- **测试接口**: 登录 → 商品详情 → 秒杀
- **期望指标**:
  - 秒杀接口响应时间 < 2000ms
  - 系统不出现崩溃
  - 数据一致性正确（库存不超卖）

### 2.3 混合场景测试
**目标**: 模拟真实用户行为
- **80%用户**: 正常浏览（商品列表→详情）
- **20%用户**: 参与秒杀
- **并发用户数**: 2000
- **测试时长**: 30分钟

### 2.4 容量规划测试
**目标**: 找到系统最大承载能力
- **并发用户数**: 100 → 500 → 1000 → 2000 → 5000
- **每个阶段**: 持续10分钟
- **监控指标**: CPU、内存、数据库连接数、响应时间

## 3. JMeter压测执行

### 3.1 启动应用
```bash
cd miaosha-v1
mvn spring-boot:run

# 或者打包后运行
mvn clean package
java -jar target/miaosha-1.0-SNAPSHOT.jar
```

### 3.2 运行JMeter测试
```bash
# 命令行模式运行（推荐）
jmeter -n -t miaosha-performance-test.jmx -l results.jtl

# 图形界面模式（调试用）
jmeter -t miaosha-performance-test.jmx

# 生成HTML报告
jmeter -g results.jtl -o report/
```

### 3.3 关键配置调整
```properties
# jmeter.properties 关键配置
# 增加堆内存
-Xms1g -Xmx4g

# 启用分布式测试（如需要）
remote_hosts=192.168.1.100,192.168.1.101

# 结果采样配置
jmeter.save.saveservice.timestamp_format=yyyy/MM/dd HH:mm:ss
jmeter.save.saveservice.response_data=false
```

## 4. 监控指标收集

### 4.1 应用层监控
```bash
# JVM监控
jstat -gc -t <pid> 1s

# 线程监控  
jstack <pid> > thread_dump.txt

# 内存监控
jmap -dump:format=b,file=heap_dump.hprof <pid>
```

### 4.2 数据库监控
```sql
-- MySQL性能监控
SHOW PROCESSLIST;
SHOW ENGINE INNODB STATUS;

-- 慢查询日志
SET GLOBAL slow_query_log = 'ON';
SET GLOBAL long_query_time = 1;

-- 连接数监控
SHOW STATUS LIKE 'Connections';
SHOW STATUS LIKE 'Threads_connected';
```

### 4.3 Redis监控
```bash
# Redis连接监控
redis-cli info clients

# 内存使用
redis-cli info memory

# 命令统计
redis-cli info commandstats
```

### 4.4 系统资源监控
```bash
# CPU使用率
top -p <java_pid>

# 内存使用
free -h

# 磁盘IO
iostat -x 1

# 网络连接
netstat -an | grep :8080
```

## 5. 性能基准与优化建议

### 5.1 关键性能指标
| 指标 | 优秀 | 良好 | 一般 | 需优化 |
|------|------|------|------|--------|
| 平均响应时间 | <200ms | <500ms | <1000ms | >1000ms |
| 95%响应时间 | <500ms | <1000ms | <2000ms | >2000ms |
| 错误率 | <0.01% | <0.1% | <1% | >1% |
| 秒杀TPS | >1000 | >500 | >200 | <200 |
| 系统CPU | <70% | <80% | <90% | >90% |
| 数据库连接数 | <50% | <70% | <80% | >80% |

### 5.2 常见性能瓶颈及解决方案

#### 5.2.1 数据库瓶颈
**现象**: 数据库连接数告警，SQL执行缓慢
**解决方案**:
```sql
-- 添加索引
ALTER TABLE miaosha_order ADD INDEX idx_user_goods (user_id, goods_id);
ALTER TABLE order_info ADD INDEX idx_user_id (user_id);

-- 优化查询
-- 避免SELECT *，只查询必要字段
-- 使用批量插入替代单条插入
```

#### 5.2.2 缓存击穿
**现象**: Redis CPU使用率高，数据库压力大
**解决方案**:
- 设置合理的缓存过期时间
- 使用布隆过滤器防止缓存穿透
- 实现分布式锁防止缓存击穿

#### 5.2.3 JVM性能问题
**现象**: GC频繁，应用响应时间不稳定
**解决方案**:
```bash
# 优化JVM参数
-Xms4g -Xmx4g
-XX:+UseG1GC 
-XX:MaxGCPauseMillis=200
-XX:+PrintGC -XX:+PrintGCDetails
```

### 5.3 系统优化方向
1. **接口层优化**: 参数校验、幂等性设计、限流熔断
2. **业务层优化**: 异步处理、消息队列削峰填谷  
3. **数据层优化**: 读写分离、分库分表、缓存策略
4. **架构层优化**: 负载均衡、CDN加速、容器化部署

## 6. 压测报告模板

### 6.1 测试环境
- **应用服务器**: CPU核数、内存大小、JVM版本
- **数据库服务器**: MySQL版本、配置参数
- **缓存服务器**: Redis版本、内存配置
- **网络环境**: 带宽、延迟情况

### 6.2 测试结果汇总
- **并发用户数**: 实际测试的并发数
- **测试时长**: 每个场景的测试时间
- **总请求数**: 发送的总请求数量
- **成功率**: 成功请求占比
- **平均TPS**: 每秒事务处理数
- **响应时间分布**: 平均值、中位数、95%、99%分位值

### 6.3 问题分析
- **性能瓶颈点**: 识别出的主要瓶颈
- **错误分析**: 错误类型和原因分析
- **资源使用情况**: CPU、内存、网络、磁盘使用率

### 6.4 优化建议
- **短期优化**: 立即可以实施的优化措施
- **中期规划**: 需要开发改造的优化方案  
- **长期架构**: 系统架构层面的优化方向

## 7. 常见问题排查

### 7.1 连接数不够
```bash
# 检查MySQL最大连接数
SHOW VARIABLES LIKE 'max_connections';

# 临时调整（重启失效）
SET GLOBAL max_connections = 1000;
```

### 7.2 内存溢出
```bash
# 检查JVM堆内存使用
jstat -gc <pid>

# 生成堆转储文件分析
jmap -dump:format=b,file=heap.hprof <pid>
```

### 7.3 秒杀超卖问题
```sql
-- 检查库存一致性
SELECT g.goods_stock, mg.stock_count, 
       COUNT(mo.id) as sold_count
FROM goods g
JOIN miaosha_goods mg ON g.id = mg.goods_id  
LEFT JOIN miaosha_order mo ON mg.id = mo.goods_id
WHERE g.id = 1
GROUP BY g.id;
```

## 8. 扩展压测场景

### 8.1 分布式压测
使用多台机器进行分布式压测，模拟更大规模的并发访问。

### 8.2 长时间稳定性测试  
7x24小时持续运行，检验系统长期稳定性。

### 8.3 异常场景测试
- 数据库连接中断
- Redis服务重启
- 网络延迟波动

通过这个完整的压测方案，你可以全面评估秒杀系统的性能表现，识别瓶颈并进行针对性优化。